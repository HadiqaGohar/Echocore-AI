version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/audio:/app/audio
      - ./backend/echocore.db:/app/echocore.db
    restart: unless-stopped
